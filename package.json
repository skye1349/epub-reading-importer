{
  "name": "epub-reading-importer",
  "version": "0.1.13",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node esbuild.config.mjs --watch",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "esbuild": "^0.28.1",
    "obsidian": "^1.8.7",
    "typescript": "^5.9.0"
  },
  "dependencies": {
    "epubjs": "^0.3.93"
  },
  "overrides": {
    "@xmldom/xmldom": "^0.9.10"
  }
}
